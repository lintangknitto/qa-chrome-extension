<!--
Skeleton for the markdown file this skill produces. Fill every section
from the actual code that was read — never from assumption. Delete any
section that doesn't apply to the scope being explained (e.g. a
single-feature explanation doesn't need "Repo layout").
-->

# {{Plain-language title — what this is, not the repo's internal name}}

*Last checked against the code on {{date}}. If the code has changed since,
regenerate this instead of hand-editing it stale.*

## In one paragraph

{{2-4 sentences, no jargon, answering: what does this do and why does it
exist. A non-technical reader should be able to stop here and already
understand the gist.}}

## How it works

{{Walk through the actual flow in plain language, in the order a user/
request/data would move through it. Use an analogy if it genuinely
clarifies something (a queue is like a line at a checkout) — skip analogies
that don't add real clarity. Reference real file/function names in
parentheses so a developer reading the same doc can jump to the code, but
don't make the reader parse code to follow the explanation.}}

## Repo layout

{{Only for a whole-repo explanation, skip for a single-feature one. A short
table or list: top-level folder → what lives there, in plain terms.}}

## Terms used above

{{Only include a term here if it was actually used above and isn't
avoidable. One line per term — the plain meaning, not a textbook
definition. Delete this section entirely if nothing above needed it.}}

## Questions this doesn't answer yet

{{Optional — note anything the reader might reasonably ask that this
explanation didn't cover, so it's clear the gap is a scoping choice, not
something missed by accident.}}
